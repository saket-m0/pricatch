import styled from "styled-components";

export const StyledFooter = styled.div`
    * {
        padding: 0;
        margin: 0;
        box-style: border-box;
    }

    .footer {
        background: #29323c;
    }

    .credit-text {
        padding: 20px;
        text-align: left;
        color: #aeb6be;
    }
`;
